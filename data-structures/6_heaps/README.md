# HEAPS

Min heaps are trees where the parent node values are smaller or equal to child nodes and Max heaps are the reverse.
The below is a binary min heap: (A parent can have max 2 children)

<img src="./images/heap.png" width="500" />